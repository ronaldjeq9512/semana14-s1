import { defineConfig } from 'vite'

export default defineConfig({
  base: '/semana14-s1/',  // Reemplaza 'NOMBRE_DEL_REPOSITORIO' con el nombre de tu repositorio
  build: {
    outDir: 'dist',
  }
})